[project]
name = "spasht-ai-backend"
version = "1.0.0"
description = "FastAPI backend for the SPASHT AI emergency dispatch prototype."
requires-python = ">=3.10"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "langdetect",
]

[tool.setuptools.packages.find]
where = ["src"]
